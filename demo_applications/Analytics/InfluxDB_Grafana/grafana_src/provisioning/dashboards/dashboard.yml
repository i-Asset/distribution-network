apiVersion: 1

providers:
- name: 'docker provisioned'
  org_id: 1
  folder: 'Provisioned'
  # should be missing to make foldersFromFilesStructure work
  type: 'file'
  editable: true
  allowUiUpdates: true
  updateIntervalSeconds: 10
  options:
    path: '/var/lib/grafana/dashboards'
#    foldersFromFilesStructure: true
